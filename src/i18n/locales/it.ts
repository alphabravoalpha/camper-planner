// Italian translations (Italiano)
// TODO: Italian translations — currently uses English as placeholder

import en from './en';

const it = {
  translation: {
    ...en.translation,
  },
};

export default it;
